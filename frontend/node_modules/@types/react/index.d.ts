import { FaRegCheckCircle } from "react-icons/fa";

const ServicesInfo = () => {
  return (
    <section className="bg-gradient-to-r from-blue-50 to-teal-50 py-16 px-6 md:px-12 lg:px-20">
      <div className="max-w-7xl mx-auto text-center">
        <h2 className="text-4xl md:text-5xl font-bold text-gray-800 leading-tight">
          Experience <span className="text-teal-600">Top-Notch Dental Care</span> 
        </h2>
        <p className="text-gray-600 mt-3 text-lg">
          Your smile deserves the best – quality treatments, flexible hours, and easy financing.
        </p>
        <div className="w-20 h-1 bg-teal-500 mx-auto my-4 rounded-full"></div>
      </div>

      {/* Updated Grid Layout */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
        {/* Left Column (Services & Opening Hours in 2 Rows) */}
        <div className="md:col-span-2 space-y-8">
          {/* Services Card */}
          <div className="bg-white shadow-md rounded-2xl p-8 hover:shadow-xl transition duration-300">
            <h3 className="text-2xl font-semibold text-gray-700 mb-4">Our Services</h3>
            <ul className="space-y-3 text-gray-600">
              {["Cosmetic Dentistry", "Crowns & Bridges", "Dental Implants", "Laser Gum Surgery", "Invisalign", "Teeth Whitening"].map((service, i) => (
                <li key={i} className="flex items-center text-lg">
                  <FaRegCheckCircle className="text-teal-500 mr-2" />
                  {service}
                </li>
              ))}
            </ul>
            <button className="mt-6 bg-teal-500 text-white py-3 px-5 rounded-lg font-semibold hover:bg-teal-600 transition duration-300">
              Learn More
            </button>
          </div>

          {/* Opening Hours Card */}
          <div className="bg-white shadow-md rounded-2xl p-8 hover:shadow-xl transition duration-300">
            <h3 className="text-2xl font-semibold text-gray-700 mb-4">Opening Hours</h3>
            <ul className="space-y-3 text-gray-600">
              {["Mon-Tue: 8:00am - 5:00pm", "Fri-Sat: 8:00am - 2:00pm", "Wed-Thu: Closed", "Sunday: Closed"].map((time, i) => (
                <li key={i} className="flex items-center text-lg">
                  <FaRegCheckCircle className="text-teal-500 mr-2" />
                  {time}
                </li>
              ))}
            </ul>
          </div>
        </div>

        {/* Right Column (Insurance Card) */}
        <div className="bg-white shadow-md rounded-2xl p-8 hover:shadow-xl transition duration-300">
          <h3 className="text-2xl font-semibold text-gray-700 mb-4">Insurance Partners</h3>
          <ul className="space-y-3 text-gray-600">
            {["Ameritas", "Cigna", "GEHA", "Guardian", "Humana", "Lincoln Financial", "Principal", "UHC", "*No Medicaid Patients"].map((insurance, i) => (
              <li key={i} className="flex items-center text-lg">
                <FaRegCheckCircle className="text-teal-500 mr-2" />
                {insurance}
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>
  );
};

export default ServicesInfo;
